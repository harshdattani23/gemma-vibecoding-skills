{
  "$schema": "https://skills.sh/schemas/skills.sh.schema.json",
  "notGrouped": "bottom",
  "groupings": [
    {
      "title": "Local-model coding",
      "description": "Save frontier tokens for thinking: the agent plans, specs, and reviews while a free local model (Gemma, Qwen, anything ollama/LM Studio/llama.cpp can serve) writes every line of application code. Tight specs get small open models writing code they never could from a vague prompt — zero API cost, and code never leaves your machine by default.",
      "skills": [
        "gemma-coder"
      ]
    }
  ]
}
